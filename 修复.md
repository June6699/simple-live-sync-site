# 修复记录

## 2026-08-30

### 调用统计地图

- 修复世界地图出现横贯画布直线的问题。原因是跨越反子午线的多边形边界被直接连接；现使用 `d3-geo` 的等距圆柱投影和反子午线裁剪后再交给 ECharts 渲染。
- 新增世界地图投影回归测试，检查地图边界不会产生异常的超长横向跳跃，并验证普通经纬度可正确投影和反投影。
- 地图数据加载失败后允许重新请求，避免一次资源加载失败导致本次页面生命周期内永久无法恢复。
- 补充 Kosovo、N. Cyprus、Somaliland 等地图源名称的国家代码兜底，减少有统计数据但地图无法匹配的情况。
- `ZZ` 未知地区不再被当作可绘制地图数据；只有未知地区数据时显示正确的空状态。
- 关闭 ECharts 无障碍自动斜纹，避免地图颜色被纹理干扰；切换指标或地图层级时恢复正确的提示文字。

### 统计数据与 Schema 兼容

- 将统计 Schema 升级到 v2，新增小时级地理聚合表和小时级访客表，使近 24 小时的国家、地区及独立访客统计能够使用小时粒度。
- 新事件同时维护既有日表和新增小时表；数据清理逻辑同步覆盖小时级地理和访客数据。
- Schema 初始化采用增量 `CREATE TABLE/INDEX IF NOT EXISTS`，保留 v1 表和历史行；发现数据库版本高于当前程序支持版本时直接拒绝启动统计模块，避免旧程序误写新库。
- 新增 `hourly_dimensions_coverage_started_at` 覆盖水位。v1 升级后的过渡窗口继续读取日级历史，避免第一条小时记录写入后旧统计突然消失。
- 覆盖水位从升级后的下一个完整 UTC 小时开始，避免把升级发生的残缺小时误认为完整数据。
- 查询窗口完全进入小时表覆盖范围后自动切换到小时表；任一查询只选择日表或小时表，不跨表相加，避免重复计数。
- availability 公共查询改为严格只读，不再因查看统计而推进水位或产生数据库写入。
- 用真实 v1 内存 Schema 验证增量升级、旧行保留、过渡期查询和覆盖完成后的自动切换；使用 SQLite `total_changes()` 验证 availability 查询没有写库。

### 房间与 WebSocket 稳定性

- JSON 能解析但结果为 `null`、数组或基础类型时返回 `invalidMessage`，避免后续访问字段导致异常。
- 将“离开房间”和“断开连接”拆分处理。客户端离开房间、房间被销毁或切换房间时，仅清理房间会话，不再丢失连接上下文、来源地区和访客哈希。
- 离开房间后仍保持连接的 WebSocket 继续接受心跳超时监管，避免失去生命周期管理。
- 重复加入当前房间改为幂等重试：返回原有用户和房间信息，不重复加入成员、不重复记录调用，也不会把创建者身份改成普通成员。
- 增加房间上下文保留、重复加入、异常 JSON 和心跳监管回归测试。

### 代理与地理识别

- Node 服务默认不信任客户端直接提供的转发 IP 和地理请求头，防止公网客户端伪造来源。
- 增加 `TRUST_PROXY_HEADERS` 配置；只有明确启用可信代理，或请求直接来自本机回环地址时，才采用代理覆盖后的来源头。
- Docker Compose 仅把服务端口发布到宿主机回环地址，并显式启用可信代理头，使宿主机 Nginx 提供的真实 IP、国家和地区能够进入统计。
- 部署文档补充可信代理的安全边界，明确直接暴露 Node 端口时不得启用该选项。

### 统计服务降级与资源释放

- Node SQLite 打开后的 PRAGMA、权限设置或 Schema 初始化失败时立即关闭句柄，避免残留数据库连接。
- 统计初始化失败时同步服务继续启动；健康检查和房间同步保持可用，统计 API 返回 `503`，实现统计模块故障时的 fail-open。
- 保留原有注入行为：提供 `metricsService` 时默认启用统计；显式设置 `metricsEnabled: false` 时优先禁用，不记录事件也不启动探针。
- Cloudflare Worker 在 `METRICS_ENABLED=false` 时不获取 Metrics Durable Object、不运行定时探针，也不初始化统计存储。
- Metrics Durable Object 改为按需初始化存储，关闭统计时不会因构造对象触发建表或元数据写入。
- 修复 Node availability 探针在服务停止时仍可能再次调度定时器的问题。

### 统计 API

- Node 和 Cloudflare Worker 的统计接口仅允许 `GET` 与 `HEAD`；其他方法返回 `405` 并设置 `Allow: GET, HEAD`。
- 重复的 `range` 查询参数不再静默取第一个值，而是返回 `400`。
- 仅把明确的范围输入错误归类为 `400`；存储层 `TypeError` 继续交给外层处理并返回通用 `503`，不再泄露内部异常或误报为客户端输入错误。
- 只有成功的 `200` 统计响应允许使用统计缓存策略；所有 `400`、`405` 和 `503` 响应统一使用 `Cache-Control: no-store`，避免服务恢复后仍命中旧错误缓存。

### 部署与 Smoke 检查

- `deploy/2.2-public-smoke.mjs` 新增 `--ping-only`，仅检查 WebSocket 建连和 ping/pong，不创建房间、不加入房间、不产生业务调用统计。
- 自动部署及公开验证脚本统一改用 `--ping-only`，并从自动验证中移除会产生真实业务调用的后端隔离检查。
- 完整业务 smoke 仍可人工执行；其会产生 6 次真实业务调用。后端隔离检查会在两个后端各产生 1 次房间创建，因此明确保留为人工诊断命令。
- smoke 脚本会拒绝未知参数和多个端点，防止把拼错的 `--ping-only` 当成普通业务检查执行。
- 部署文档同步说明自动检查和人工诊断的差异。

### 数据库安全

- 本次修复没有删除、清空、重建或覆盖任何现有数据库，也没有执行 `DROP TABLE`、`TRUNCATE` 或清库操作。
- 修复和测试期间没有打开仓库内已有的 `.sqlite` 文件；统计测试只使用 `:memory:` 或测试框架新建的隔离数据文件。
- v1 到 v2 只增量创建新表、索引和元数据，既有日级统计行继续保留。
- v1 只保存日级地理和访客数据，无法还原精确小时。升级后的短暂过渡期，近 24 小时统计会按重叠 UTC 日近似，可能带入额外日内数据；查询窗口越过覆盖水位后会自动恢复精确小时统计。

### 验证结果

- `npm run typecheck`：通过。
- `npm test`：5 个测试文件、29 项测试全部通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- `node --check deploy/2.2-public-smoke.mjs`：通过。
- 4 个修改过的 Bash 脚本均通过 `bash -n`。
- 本地内存 SQLite 集成验证确认：执行 `--ping-only` 前后业务调用数均为 0。

## 2026-09-25

### 域名迁移：sync.furry.mo.cn → june6699.top

- 排查确认 `sync.furry.mo.cn` 不可用的根因：nginx 引导阶段写入的自签名证书（issuer=subject，且无 SAN 扩展）一直未替换为正式 Let's Encrypt 证书，所有 TLS 客户端（含 App 的 wss 连接）握手即被拒绝；80 端口正常仅说明 nginx 存活，不涉及业务可用性。
- june6699.top 是本项目部署脚本与文档的原始目标域名；其 certbot lineage 已存在但 `fullchain.pem` 与 `cert+chain` 不匹配导致 renewal 报错跳过。使用 `certbot certonly --nginx -d june6699.top -d www.june6699.top --force-renewal` 重新签发成功（ECDSA，2026-12-24 到期，自动续期）。
- 遵循"只保留一个域名部署"原则：`sites-enabled/sync.furry.mo.cn.conf` 已移除（备份于 `/var/backups/simple-live-sync/nginx/`），新配置位于 `/etc/nginx/conf.d/june6699.top.conf`（含 www），仓库 `deploy/nginx/june6699.top.conf` 已同步为实际生效版本。
- 容器 `PUBLIC_ORIGIN` 通过 `/opt/simple-live-sync/.env` 改为 `https://june6699.top`。
- 同步服务器源码此前落后于仓库（无 `sendDouyuAccount` 等新 action，实测返回 `unknownType`）。已用本地构建的 `dist`/`public` 覆盖 `/opt/simple-live-sync`，并通过 compose 只读 bind-mount 挂载进容器（`dist:/app/dist:ro`、`public:/app/public:ro`）。今后代码更新不再需要 docker build，详见 DEPLOYMENT.md "Code-only updates"。
- 验证：`https://june6699.top/health` 与 `https://www.june6699.top/health` 200；`wss://june6699.top/sync` 双连接房间中继实测 `sendDouyuAccount` → `douyuAccountReceived` 成功；`sync.furry.mo.cn` 已不再提供 TLS（预期）。
- Cloudflare Worker（GitHub push 自动部署）为独立备用线路，不受本次迁移影响；本机网络直连 workers.dev 超时属预期（需代理）。
